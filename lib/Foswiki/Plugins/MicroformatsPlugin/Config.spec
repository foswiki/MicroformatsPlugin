#---+ MicroformatsPlugin
# **BOOLEAN**
# enable adding http://microid.org 's to the view meta when rendering a user's topic
$Foswiki::cfg{Plugins}{MicroformatsPlugin}{enableMicroIds} = $TRUE;

# **BOOLEAN**
# enable adding hCards to the view when rendering a user's topic
$Foswiki::cfg{Plugins}{MicroformatsPlugin}{enablehCardOnUserTopic} = $TRUE;

